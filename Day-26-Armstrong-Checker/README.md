# Day 26: Armstrong Number Checker 🔢

A mathematical script to verify if a given number is an Armstrong number.

## 📝 Description
This program takes a number as input and determines if the sum of its own digits each raised to the power of the number of digits is equal to the number itself. It works dynamically for any number of digits (3-digit, 4-digit, etc.).

## 🛠️ Concepts Used
* **Power Operator (`**`):** Used to raise each digit to the power of the number's length.
* **For Loops:** Iterating through each digit of the number.
* **len() Function:** Dynamically finding the total number of digits for the exponent.
* **Type Conversion:** Handling string input and converting digits to integers for math.

## 🚀 How to Run
```bash
python main.py