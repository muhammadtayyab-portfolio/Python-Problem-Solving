# Day 14: Largest Number Finder 🔢

A comparison script to find the maximum value among three user-provided numbers without using built-in functions.

## 📝 Description
This project focuses on manual comparison logic. It uses a temporary variable to track the largest number across multiple `if` statements.

## 🛠️ Concepts Used
* **Conditional Comparison:** Comparing variables manually.
* **Algorithm Logic:** Tracking a "max" value throughout the flow.