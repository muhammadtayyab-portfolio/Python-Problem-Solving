# Day 21: Palindrome Checker 🔄

A string manipulation project that determines if a word is a palindrome (reads the same forward and backward).

## 📝 Description
This program takes a string input and reverses it manually using a `while` loop and negative iteration. It then compares the original string with the reversed version to check for symmetry. 

## 🛠️ Concepts Used
* **String Iteration:** Using a manual counter to traverse from the last index to zero.
* **String Concatenation:** Building the reversed string character by character.
* **Boolean Logic:** Using an `if-else` block to validate the palindrome property.

## 🚀 How to Run
```bash
python main.py