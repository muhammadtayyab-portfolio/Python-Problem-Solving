# Day 35: Heads or Tails Toss Game 🪙

A simple interactive game that simulates a coin toss using Python's random module.

## 📝 Description
This script allows a user to pick "Heads" or "Tails". The program then uses a random number generator to determine the winner. It's a great project for understanding basic conditional logic and randomization in Python.

## 🛠️ Concepts Used
* **Random Module:** Utilizing `random.randint(0, 1)` to simulate a 50/50 chance.
* **Input Normalization:** Using `.lower()` to handle user input regardless of capitalization.
* **Conditional Logic:** Using `if-else` statements to assign the opponent's pick and declare the winner.

## 🚀 How to Run
```bash
python main.py