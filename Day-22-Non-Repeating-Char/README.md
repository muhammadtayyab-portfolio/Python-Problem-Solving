# Day 22: First Non-Repeating Character 🥇

An optimized search to find the first character in a string that does not repeat.

## 📝 Description
The program scans the input string and uses the `.count()` method to find the first character with a frequency of exactly 1.

## 🛠️ Concepts Used
* **String Methods:** Using `.count()` for frequency.
* **Loop Control:** Using `break` to stop at the first match.